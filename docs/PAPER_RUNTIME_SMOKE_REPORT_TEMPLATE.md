# Paper Runtime Smoke Report Template

実施日時:

Paper version:

Java version:

ReputationBan jar:

JAR sha256:

起動結果:

`/plugins`確認:

`/rep version`:

`/rep doctor`:

`/rep help`:

`/reportbad TAB`:

`/reports help`:

`/rep audit recent`:

`/rep audit export recent`:

`/rep backup`:

`/rep support bundle`:

`/rep maintenance preview`:

Discord webhook disabled default確認:

Webhook URLがログに出ていない確認:

BAN系はテストユーザーのみ:

結果: PASS/FAIL

未解決事項:
