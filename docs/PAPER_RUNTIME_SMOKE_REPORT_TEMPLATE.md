# Paper Runtime Smoke Report Template

PaperMC 26.1.2 / Java 25 の test server で実行した結果を、そのまま記入してください。Webhook URL などの secret は書かないでください。

実施日時:

実施者:

Paper version:

Java version:

ReputationBan jar:

JAR sha256:

起動結果:

`/plugins` 確認:

`/rep version`:

`/rep doctor`:

`/rep help`:

`/reportbad TAB`:

`/reports help`:

`/rep audit recent`:

`/rep audit export recent`:

`/rep backup`:

`/rep support bundle`:

`/rep maintenance preview`:

Discord webhook disabled default 確認:

Webhook URL が logs、`/rep doctor`、audit output、CSV output、support bundle に出ていない確認:

`config-redacted.yml` 共有前確認:

BAN 系は test users のみ:

結果: PASS/FAIL

未解決事項:
